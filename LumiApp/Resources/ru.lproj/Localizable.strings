/* Placeholder — UI strings are currently hardcoded in Russian directly in
   SwiftUI views rather than routed through NSLocalizedString/String(localized:).
   Wire this up if/when the app needs a second language. */
