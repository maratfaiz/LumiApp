import Foundation

/// Текст политики конфиденциальности внутри приложения.
///
/// Источник — `docs/legal/Privacy_Policy_RU.md`; здесь короткая версия для
/// экрана настроек, чтобы её можно было прочитать без интернета (и до
/// того, как появится сайт). При изменении текста меняем обе версии.
enum PrivacyPolicy {
    static let version = "1.0"
    static let effectiveDate = "12 августа 2026"

    struct Section: Identifiable {
        let id = UUID()
        let title: String
        let body: String
    }

    static let summary = "Все данные Луми остаются на твоём устройстве. У приложения нет серверов: ответы в упражнениях и записи дневника никуда не отправляются, аналитики и рекламы нет."

    static let sections: [Section] = [
        Section(
            title: "Что хранится",
            body: "Имя, ответы на вопросы онбординга, ответы в упражнениях, записи дневника эмоций, избранные аффирмации и прогресс (уровень, люмены, серия, покупки). Всё это лежит в памяти устройства и в общем контейнере с виджетом — чтобы виджет показывал серию и текущий урок."
        ),
        Section(
            title: "Вход через Google",
            body: "Из профиля Google Луми берёт только имя — чтобы знать, как к тебе обращаться, — и хранит его на устройстве. E-mail не сохраняется, аккаунт на нашей стороне не создаётся, токены входа никуда не отправляются. Сам факт входа Google обрабатывает по своей политике конфиденциальности."
        ),
        Section(
            title: "Кризисная проверка",
            body: "Тексты упражнений и дневника сверяются со списком тревожных формулировок прямо на устройстве. Это сопоставление с фразами, а не искусственный интеллект и не диагноз. Результат нужен только для того, чтобы показать телефоны помощи: он не сохраняется и никуда не передаётся, и никакая служба автоматически не вызывается."
        ),
        Section(
            title: "Чего мы не собираем",
            body: "Геолокацию, контакты, фото, микрофон, календарь, рекламные идентификаторы. Трекеров и аналитики в приложении нет."
        ),
        Section(
            title: "Удаление данных",
            body: "Записи дневника и избранные фразы удаляются по одной прямо в приложении. Удаление приложения стирает все данные безвозвратно. Записи можно выгрузить текстом через «Поделиться»."
        ),
        Section(
            title: "Важно",
            body: "Луми не медицинское приложение: он не ставит диагнозы и не заменяет психолога или психиатра. Материалы носят обучающий и поддерживающий характер."
        ),
    ]
}
